<nav class="nav">
    <div class="nav-group"></div>
    <ul class="nav-group large">
        <li><a href="/" class="nav-link">Daily Puzzle</a></li>
        <li><a href="/practice" class="nav-link">Practice Mode</a></li>
    </ul>
    <ul class="nav-group right">
        <li><a href="/patch-notes" class="nav-link">Patch Notes</a></li>
    </ul>
</nav>

<style>
    .nav {
        display: flex;
        justify-content: space-between;
        gap: var(--spacing-06);
    }
    .nav-group {
        list-style: none;
        margin: 0;
        padding: 0;
        justify-content: center;
        flex-basis: 20%;
        display: flex;
        flex-grow: 1;
        gap: var(--spacing-06);
        &.large {
            flex-basis: 60%;
        }
        &.right {
            justify-content: flex-end;
        }
        &.left {
            justify-content: flex-start;
        }
    }
    .nav-link {
        color: var(--text-normal);
        transition: color 0.2s ease-in-out;
        text-decoration: none;
        &:hover {
            color: var(--text-hover);
        }
    }
    @media (width < 600px) {
        .nav-group {
            flex-basis: unset;
            flex-wrap: wrap;
            flex-grow: 0;
            &.large {
                flex-basis: unset;
            }
        }
        .nav {
            justify-content: center;
            flex-wrap: wrap;
            & a {
                display: block;
            }
        }
    }
    @media (width < 400px) {
        .nav-group {
            flex-direction: column;
            align-items: center;
        }
        .nav {
            align-items: center;
            flex-direction: column;
        }
    }
</style>
